from quactrl.persistence import (
    Control
    )


class InspectionManager:
    """Manage to follow a control plan for a defined process and batch"""
    def __ini__(self, environment):
        self.env = environment
        self.devices = {}
        self.inspectors = {}

    def setup_session(self, process):
        """Load devices for inspector"""
        pass

    def setup_batch(self, batch):
        """Load controls and their inspector for a batch"""
        self.batch = batch
        

    def start_inspection(self):
        pass

    def check_inspected(self):
        pass

    def check_started(self, check):
        self.env.view.notify(check)

